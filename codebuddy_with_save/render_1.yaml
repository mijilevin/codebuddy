services:
  - type: web
    name: codebuddy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
